{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.10.1"
  },
  "scripts": {
    "compile": "npx hardhat compile",
    "flatten": "npx hardhat flatten > flattened.sol",
    "deploy-local": "npx hardhat run scripts/deploy.js",
    "deploy-testnet": "npx hardhat run scripts/deploy.js --network eth_testnet",
    "deploy-mainnet": "npx hardhat run scripts/deploy.js --network eth_mainnet"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.7.1"
  }
}
